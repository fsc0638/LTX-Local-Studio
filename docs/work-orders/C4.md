# C4 門檻校準

規格：`docs/PRODUCTION_ROADMAP.md` 的 C4 節。分支 `wo/c4`。
依 2026-09-06 的建置原則（架構先建、素材後補）：結構全部做完、用手寫 fixture 驗；真素材的校準跑分列在「待素材測試」。

## 已完成

- `lib/calibration.ts` — 讀 `calibrate_embeddings.py` 的報告：`fpr_05` → 預設線、`fpr_01` → `strict.*`；
  `face_facenet → cj`、`dinov2_large → cj_dino`、`clip_vit_l14 → sj`；`lab_mean_delta_e` 方向相反不匯入。
  **只有 `note` 的 metric 不寫、不猜**，回報 `skipped`；`calibrated` = (cj 或 cj_dino) 且 sj 有量到。
  第二份較小的報告不會抹掉第一份量過的線（`existing` 合併）。`report` 摘要帶 `missing_faces`、`lines`、`eer`、`imported_at`。
- `lib/production-factory.ts` — `thresholds` 進 Bible 白名單與型別（原本未知鍵會被 `normalizeFactoryBible` 丟出錯誤）。
- **`cj` 與 `cj_dino` 分線**（`review_rules.py`＋`lib/review.ts`）：facenet 與 DINOv2 的相似度尺度不同，
  一條線不能兩用。燈號用該 take 多數幀的方法選線；抽屜曲線每一秒依自己的方法著色。
  兩邊測試共用 DINO fixture（同一個 median 在臉線是紅、在 dino 線是綠——證明線真的不同）。
- `components/production-factory.tsx` — Bible 面板「裁判門檻」區：匯入校準報告（檔案選取、解析、寫進
  `bible.thresholds`）、目前四條線與嚴格值、未校準／已校準狀態、**臉部線仍為暫定**提示、
  **無臉圖清單**（來自報告的 `missing_faces`，可展開）。三語文案。
- `components/review-board.tsx` — cj 分數條與曲線用該 take 的線；抽屜覆寫多 `cj_dino` 一列；
  鏡頭列標示臉部線未校準。
- 測試：`tests/calibration.test.mjs`（7）、`tests/review.test.mjs`（+2）、`test_factory_review.py`（+1）。

## 待素材測試（架構已就緒，等 calibration/）

| 需要 | 怎麼跑 | 預期 |
|---|---|---|
| `calibration/<角色>/` ≥2 角色、每個 ≥10 張，放主 checkout（已 gitignore） | `HF_HOME=/opt/studio/models/hf TORCH_HOME=/opt/studio/models/torch /opt/studio/venvs/vision/bin/python infra/gb10/tools/calibrate_embeddings.py --root "/home/kwayrdc/LTX Local Studio/calibration" --out calibration_report.json` | 四個 metric 各有 fpr_01／fpr_05／eer；無臉圖列在 `missing_faces` |
| 上一步的報告 | 00 企劃 Bible →「匯入校準報告」 | 狀態變「已校準」，04 的「未校準」banner 消失；若臉太少則顯示「臉部線仍為暫定」 |
| 一個有參照圖的專案跑出新 take | 04 審片 | 有臉的幀走 `cj`、無臉的走 `cj_dino`，曲線點色依各自的線 |

## 需要阿寶做的事

- 素材到位時傳「C4 · 繼續」，我跑上表第一列並匯入。不需要現在做任何事。
