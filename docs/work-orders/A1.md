# 工單 A1：Bible 與繼承

## 進度

### 已完成

- 建立 `wo/a1`，基準為 `origin/main` 的 `b0aa061`。
- 工作單格式升級為 v2：`plan.bible`、`shot.pinned[]` 與預設 `lyric_offset_seconds=-0.9`。
- 完成 `projectBible()`、欄位 pin／unpin、`reprojectShots()` 與覆寫鏡數統計。
- 完成 v1 → v2 匯入遷移；v2 匯出保留 Bible、完整 request 與 pinned。
- 新增 6 項 A1 純函式測試，連同既有 Factory 測試共 12 項通過。
- 更新 `docs/PRODUCTION_FACTORY.md` 的 v2 格式與繼承說明。
- 工廠頂部新增 Bible 專案面板：共用 `CharacterLock`、音樂素材選擇、LRC／起點／模式、輸出規格、導演預設與歌詞偏移。
- 「新增空白鏡頭」改為 Bible 投影的「新增鏡頭」；未設定 Bible 時只提示、不建立空鏡。
- 每鏡新增繼承／覆寫欄位籤、還原繼承，以及完整 request JSON 抽屜；有效 JSON 改動立即呼叫 `/api/v1/validate`。
- 生成頁首次「加入製片工廠」會從當下完整 request 建立 Bible。
- A1 純函式測試增至 7 項，加入 sandbox request → Bible 測試。

### 未完成

- 以已登入瀏覽器在 3001 做互動式驗收：建立角色＋音樂 Bible、新增鏡頭、修改／還原覆寫、檢查 validate 訊息。
- 驗收工作另開「工單 A1 · 驗收」，逐條留下 PASS／FAIL 證據。

### 需要阿寶做的事

- 若要在瀏覽器驗收，需要阿寶於 dev preview 登入；本輪不碰正式服務。

## 最新自檢

- `node --test tests/*.test.mjs`：27/27 PASS。
- `npx --no-install tsc --noEmit -p tsconfig.json`：PASS。
- `npm run build`：PASS。
