# 工單 A2：階段導覽與狀態板

## 子步驟

- [x] (1) `lib/stages.ts` + `components/stage-rail.tsx`：七階段列、狀態籤、手機 stepper。
- [x] (2) `components/status-board.tsx`：每個計畫一列（階段／上一步結論／下一步誰負責）。
- [ ] (3) `app/page.tsx`：TabKey 改為階段、面板重接、沙盒與工站改名、三語文案。
- [ ] (4) 完整自檢與 A2 驗收情境。

## 進度

### 已完成

- `lib/stages.ts`：`planProgress()` 由計畫資料算出七階段狀態（idle／active／attention／done／disabled）、
  目前階段、上一步結論與下一步負責人。**刻意不依賴任何模組** — 取一個 `PlanSnapshot`（hasBible、status、
  total、completed、failed）而非整個 plan，因此可單獨測試，也避開 `lib` 互相 import 需要開
  `allowImportingTsExtensions` 的問題（那會改動全專案 tsconfig 慣例，超出 A2 範圍）。
- `components/stage-rail.tsx`：左側階段列（lg 以上 sticky 直列，以下為可橫向捲動的 snap stepper），
  00–06 各一顆狀態籤；02／04／05 標「本期未啟用」並降低不透明度；沙盒與工站在階段列下方視覺降級。
  三語文案齊全。選取狀態用固定 class（不做字串拼接，否則 Tailwind 靜態擷取不到）。
- `components/status-board.tsx`：狀態板，每個計畫一列；匯出 `progressOf(plan)` 作為 plan → snapshot
  的唯一轉接點，供 `app/page.tsx` 重用。三語文案齊全。
- `tests/stages.test.mjs`：6 項測試（空計畫指向 Bible、Bible 完成後推進到分鏡、未啟用階段永不成為目前
  階段、running 交給主機、失敗鏡優先於已完成鏡並要人處理、階段順序穩定）。

### 未完成

- `app/page.tsx` 全部改動：`TabKey` 改為階段鍵、以 `StageRail` 取代 header 的五個 tab 與行動版導覽、
  版面改為左列＋內容的 grid、`StatusBoard` 作為首頁、面板重新對應
  （00＝Bible 面板＋素材庫、01＝MV 時間軸、03＝工廠佇列、06＝sequence 組片＋下載、沙盒、工站）、
  三語文案補齊。
- `ProductionFactory` 需要新增 `section?: 'all' | 'bible' | 'queue'`：階段 00 與 03 必須共用**同一個掛載
  的元件實例**（只切換顯示哪一段），否則會變成兩份獨立的計畫狀態。
- 完整自檢：`node --test tests/*.test.mjs && npx --no-install tsc --noEmit -p tsconfig.json`（目前新增的
  部分已通過，尚未跑過含 page.tsx 改動的全套）。
- 375px 寬度的 stepper 實機確認。

### 需要阿寶做的事

- 無。下一則「工單 A2 · 繼續」可直接從 `app/page.tsx` 與 `ProductionFactory` 的 `section` prop 開始。

## 本輪自檢

```
node --test tests/stages.test.mjs   → tests 6 / pass 6 / fail 0
npx tsc --noEmit -p tsconfig.json   → exit 0
```
